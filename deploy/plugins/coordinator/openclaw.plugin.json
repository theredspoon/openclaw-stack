{
  "id": "coordinator",
  "name": "Coordinator",
  "description": "Auto-discovers sub-agent routes from agent configs and writes routing table to coordinator's AGENTS.md",
  "version": "2.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "coordinatorAgent": {
        "type": "string",
        "description": "Agent ID that acts as coordinator (receives routing context)",
        "default": "main"
      },
      "routes": {
        "type": "array",
        "description": "Optional static fallback routes — only used if loadConfig() is unavailable. Normally routes are auto-discovered from agent configs.",
        "items": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Sub-agent ID" },
            "name": { "type": "string", "description": "Display name for the sub-agent" },
            "skills": { "type": "array", "items": { "type": "string" }, "description": "Skills this sub-agent handles" }
          },
          "required": ["id", "name", "skills"]
        }
      }
    }
  }
}
